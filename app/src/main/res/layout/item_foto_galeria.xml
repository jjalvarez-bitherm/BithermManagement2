<?xml version="1.0" encoding="utf-8"?>
<FrameLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="180dp"
    android:layout_height="180dp"
    android:layout_margin="4dp"
    android:background="@drawable/bg_card_gallery">

    <!-- Imagen de la foto -->
    <ImageView
        android:id="@+id/imgFoto"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:scaleType="centerCrop"
        android:src="@drawable/ic_photo_placeholder" />

    <!-- Overlay con controles (solo visible en fotos existentes) -->
    <LinearLayout
        android:id="@+id/overlayControles"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:orientation="vertical"
        android:background="#80000000"
        android:visibility="gone">

        <!-- Controles superiores -->
        <LinearLayout
            android:layout_width="match_parent"
            android:layout_height="0dp"
            android:layout_weight="1"
            android:orientation="horizontal"
            android:gravity="end|top"
            android:padding="4dp">

            <!-- Botón borrar -->
            <ImageView
                android:id="@+id/btnBorrar"
                android:layout_width="24dp"
                android:layout_height="24dp"
                android:src="@drawable/ic_delete"
                android:background="@drawable/bg_button_rounded"
                android:padding="2dp"
                android:layout_marginEnd="4dp" />

            <!-- Botón editar -->
            <ImageView
                android:id="@+id/btnEditar"
                android:layout_width="24dp"
                android:layout_height="24dp"
                android:src="@drawable/ic_edit"
                android:background="@drawable/bg_button_rounded"
                android:padding="2dp" />

        </LinearLayout>

        <!-- Etiqueta del tipo de foto -->
        <TextView
            android:id="@+id/txtTipoFoto"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:text="EQUIPO"
            android:textColor="@android:color/white"
            android:textSize="10sp"
            android:gravity="center"
            android:background="#80000000"
            android:padding="2dp" />

    </LinearLayout>

    <!-- Botón de añadir (solo visible en el último item) -->
    <LinearLayout
        android:id="@+id/btnAnadir"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:orientation="vertical"
        android:gravity="center"
        android:background="@drawable/bg_card_gallery"
        android:visibility="gone">

        <ImageView
            android:layout_width="48dp"
            android:layout_height="48dp"
            android:src="@drawable/ic_add_photo"
            android:background="@drawable/bg_button_rounded"
            android:padding="8dp" />

        <TextView
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="Añadir foto"
            android:textSize="10sp"
            android:textColor="@android:color/black"
            android:layout_marginTop="4dp" />

    </LinearLayout>

</FrameLayout> 